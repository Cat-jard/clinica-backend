server:
  port: 8003

spring:
  datasource:
    url: jdbc:postgresql://localhost:5433/sihce_triaje
    username: postgres
    password: admin
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
    show-sql: true
    open-in-view: false
  cloud:
    openfeign:
      client:
        config:
          citas-service:
            connect-timeout: 5000
            read-timeout: 5000
          recepcion-service:
            connect-timeout: 5000
            read-timeout: 5000

springdoc:
  api-docs:
    enabled: true
  swagger-ui:
    path: /swagger-ui.html

logging:
  level:
    com.triaje: DEBUG
    org.springframework.cloud.openfeign: DEBUG

citas:
  api:
    url: http://localhost:8082

recepcion:
  api:
    url: http://localhost:8081
